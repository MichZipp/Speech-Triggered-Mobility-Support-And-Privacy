package de.sick.zipperle.microsoft.speakerregonition;

/*import android.renderscript.ScriptGroup;

import com.microsoft.cognitive.speakerrecognition.SpeakerIdentificationRestClient;
import com.microsoft.cognitive.speakerrecognition.contract.GetProfileException;
import com.microsoft.cognitive.speakerrecognition.contract.identification.OperationLocation;
import com.microsoft.cognitive.speakerrecognition.contract.identification.Profile;

import java.io.IOException;
import java.io.InputStream;
import java.util.List;*/

/**
 * Created by michael on 21.12.17.
 */

public class SpeakerRecognition {

    /*private SpeakerIdentificationRestClient client = null;
    private List<Profile> profiles = null;

    private final String apiKey = "28dc898035b1441aabb2709afd354c15";

    public SpeakerRecognition(){
        client = new SpeakerIdentificationRestClient(apiKey);
        try {
            profiles = client.getProfiles();
        } catch (GetProfileException e) {
            e.printStackTrace();
        } catch (IOException e) {
            e.printStackTrace();
        }


        InputStream in = new InputStream() {
            @Override
            public int read() throws IOException {
                return 0;
            }
        };

        OperationLocation identify;
        //identify = client.identify(in, this, true);
    }*/
}
