<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      android:versionCode="1"
      android:versionName="1.0" package="ai.kitt.snowboy.demo" >
    <application android:label="@string/app_name" android:icon="@mipmap/ic_launcher">

        <activity
            android:name="ai.kitt.snowboy.Activities.SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="ai.kitt.snowboy.Activities.MainActivity"/>


        <activity android:name="ai.kitt.snowboy.Activities.TestActivity"/>

        <activity android:name="ai.kitt.snowboy.Activities.PersonalProfile"/>

        <activity android:name="ai.kitt.snowboy.Activities.BusinessProfile"/>

        <activity android:name="ai.kitt.snowboy.Activities.CreateProfileActivity"/>

        <activity android:name="ai.kitt.snowboy.Activities.ImpressumActivity"/>

        <activity android:name="ai.kitt.snowboy.Demo"/>
    </application>

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
<uses-permission android:name="android.permission.RECORD_AUDIO"></uses-permission>
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
</manifest> 
